
1. Elabore un script bash que:
Tenga el nombre: buscar_sinclave.sh
Encuentre los usuarios con UID >=1000 que no tengan una 
clave asignada.
2. Elabore un script bash que:
Solicite y reciba como input (no argumento) un numero y muestre
los X primeros ficheros (no directorios) del directorio /etc
3. Elabore un script bash que:
Reciba como parametro el url de una pagina web, descargue la misma usando 
el comando wget y muestre el tamaño en lineas del fichero.
4. Elabore un script bash que revise el fichero /etc/passwd y que por cada 
usuario con uid>=1000 que no tenga GECOS o informacion, pida el dato y lo 
asigne con el comando usermod

